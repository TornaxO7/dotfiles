groups:
- name: zpool status
  rules:
  - alert: DegradedZpool
    expr: zfs_pool_health != 0
    for: 1m
    labels:
      severity: critical
    annotations:
      title: "zpool isn't healthy!"
      description: "A zpool isn't healy anymore! Check with 'zpool status' which one failed!"